#include <stdio.h>
#include<string.h>

int main() {
   
   char str[]="Hello";
   char *pos=strchr(str,'l');
   if(!pos){
       printf("Not found");
   }
   else{
       printf("Found at %d", pos-str);
   }
    
    return 0;
}
