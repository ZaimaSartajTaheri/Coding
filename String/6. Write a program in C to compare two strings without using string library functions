#include <stdio.h>
#include<string.h>
int main() {
  char c[100];
  char d[100];
scanf("%s",c);
scanf("%s",d);
 int co=0;
  int i=0;
  if(strlen(c)!=strlen(d)){
      printf("Strings are not equal");
      return 0;
  }
  else{
      while(c[i]!='\0' || d[i]!='\0'){
    //  printf("%c\n",co);
     if(c[i]==d[i]){
         co++;  
     }
 
     i++;
 }
 if(co==strlen(c)){
      printf("Strings are equal");
 }
 else{
      printf("Strings are not equal");
 }
  }
 
//   printf("%d",co+1);
    return 0;
}
